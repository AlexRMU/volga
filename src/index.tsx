import * as React from "react";
import * as ReactDOM from "react-dom/client";
import App from "./App";

let e = document.getElementById("glasses-quiz-widget");
if (e) ReactDOM.createRoot(e).render(<App />);
